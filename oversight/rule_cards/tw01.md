# tw01 — PathDots

Draw one continuous path from the start node to the end node that passes through **all** hexagon dot waypoints. The level is solved when the path reaches the end and every dot lies on the path. Advanced levels add breakpoints (blocked edges) and multiple start nodes.

Core knowledge: Objectness (preserve/visit specific elements).
