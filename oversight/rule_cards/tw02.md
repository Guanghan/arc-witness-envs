# tw02 — ColorSplit

Draw a path that **partitions** the grid into regions such that every region contains squares of **only one color**. Two differently-colored squares may never share a region. CONFIRM when the partition separates all colors.

Core knowledge: Objectness + Numbers (classify by attribute).
