# tw09 — CylinderWrap

PathDots on a horizontally **wrapping** grid: the left edge and right edge are identified (column 0 == column `cols`). The path may exit the right edge and re-enter at the left. Visit all hexagon dots and reach the end. Hand-crafted levels only (no solver).

Core knowledge: Topology — non-planar / wrap-around space.
