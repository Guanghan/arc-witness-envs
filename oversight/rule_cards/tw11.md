# tw11 — MultiRegion

Draw a path that partitions the grid; **each** region must simultaneously satisfy **two or more** constraint types that are present (e.g. squares + triangles, stars + tetris, triangles + tetris, or 3+ combinations). All present constraints must hold per region (AND logic).

Core knowledge: Composition — multi-constraint satisfaction.
