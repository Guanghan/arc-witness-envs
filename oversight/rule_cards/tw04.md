# tw04 — SymDraw

You control the **blue** line; a **yellow** line mirrors every move automatically under the level's symmetry (`horizontal`, `vertical`, or `rotational` 180°). Both lines must reach their respective end nodes **simultaneously**, visiting any colored waypoints, without either line crossing a breakpoint.

Core knowledge: Geometry — symmetry transforms, dual-state mental simulation.
