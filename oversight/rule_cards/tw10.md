# tw10 — ColorFilter

A ColorSplit variant: **filter cells** transform the *perceived* color of squares that lie behind/within them. Region constraints (one color per region) apply to the **perceived** (post-filter) colors, not the raw colors. Hand-crafted levels only (no solver).

Core knowledge: Perception — transform-then-apply.
