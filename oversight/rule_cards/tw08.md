# tw08 — ComboBasic

Draw a path that partitions the grid; **each** region must simultaneously satisfy **both** ColorSplit (only one square color per region) **and** StarPair (each star color present appears exactly twice). Both constraints must hold at once.

Core knowledge: Composition — multiple simultaneous rules.
