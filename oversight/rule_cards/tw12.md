# tw12 — HexCombo

Draw a path that visits **all** hexagon dot waypoints (a hard path constraint) **and** partitions the grid into regions where all present region constraints (squares / stars / triangles / tetris) hold simultaneously. Both the dot-visiting and the region rules must be satisfied together.

Core knowledge: Composition — path waypoints + region rules (dual objective).
